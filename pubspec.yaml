name: inventory_application
description: A uniform inventory system app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  shared_preferences: ^2.1.0
  google_fonts: ^6.2.1
  charts_flutter_new: ^0.12.0
  fl_chart: ^0.68.0
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  google_sign_in: ^6.1.5
  http: ^1.1.0
  qr_flutter: ^4.1.0
  mailer: ^6.1.1
  path_provider: ^2.1.1
  qr_code_scanner: ^1.0.1
  mobile_scanner: ^5.1.1
  zxing2: ^0.2.4
  image_picker_web: ^4.0.0
  file_picker: ^8.0.3
  image: ^4.5.4
  qr_code_dart_scan: ^0.11.5
  permission_handler: ^11.0.1
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/

